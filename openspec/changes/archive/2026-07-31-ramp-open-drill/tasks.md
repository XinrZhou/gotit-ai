# ramp-open-drill — tasks

## A — Backend

- [x] `ToolCallRecord.open_drill` + `start_drill` + hint
- [x] Orchestrator lift `metadata.open_drill`
- [x] `InterviewUpcoming.project_id`；upcoming / get_upcoming 挂 open_drill
- [x] Tests

## B — Web

- [x] `OpenDrillPayload` + trail「深挖」
- [x] `onDrillStartWithPayload` in useDrill / store
- [x] ChatPage `followOpenDrill`

## C — Docs

- [x] SYSTEM + README；archive change
