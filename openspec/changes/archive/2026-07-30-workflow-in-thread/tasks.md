# workflow-in-thread — tasks

## Spec

- [x] proposal / design / tasks
- [x] Update `openspec/changes/README.md` active table

## Backend

- [x] `db.ops.thread.append_workflow_exchange` + barrel export
- [x] Examine / Teach / Drill routes: optional `thread_id` + persist turns
- [x] MCP tools: same optional `thread_id` (parity)
- [x] pytest: writeback + ownership; no-thread path unchanged

## Web

- [x] shell `workflowThreadId`; ChatPage bind on workflow start
- [x] useExamine / useTeach / useDrill pass `thread_id`
- [x] ChatPage badge for `metadata.workflow`
- [x] `npm run build`

## Docs

- [x] `docs/SYSTEM.md` + README roadmap（去掉 Not done / 标 Done）
