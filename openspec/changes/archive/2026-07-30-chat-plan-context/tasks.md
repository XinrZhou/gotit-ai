# chat-plan-context — tasks

## Spec

- [x] proposal / design / tasks
- [x] Update `openspec/changes/README.md` active table

## Backend

- [x] `format_today_plan_brief` + `build_chat_prompt` / `run_chat` accept brief
- [x] `post_message_chain` load today's plan and pass brief
- [x] pytest: format + prompt injection

## Docs

- [x] `docs/SYSTEM.md` + README（Chat 注入今日计划）
